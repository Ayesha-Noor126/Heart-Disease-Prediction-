# Heart-Disease-Prediction-
Machine Learning project that predicts heart disease using Logistic Regression, KNN, and Random Forest models. Includes data preprocessing, feature scaling, one-hot encoding, model evaluation, and an ensemble majority-vote prediction system.
